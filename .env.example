# SeineCrops · variables d'environnement
# Copier ce fichier en .env et renseigner les valeurs réelles.
# .env ne doit JAMAIS être commité (déjà exclu via .gitignore).

# ── PostgreSQL / PostGIS ──────────────────────────────────────────────────
PG_HOST=localhost
PG_PORT=5432
PG_DBNAME=seinecrops
PG_USER=postgres
PG_PASSWORD=
