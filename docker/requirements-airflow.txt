# SeineCrops, dépendances de l'image Airflow personnalisée.
#
# Sous-ensemble de requirements.txt : uniquement ce dont les tâches du DAG
# ont besoin (src/acquisition, src/processing, src/ml, src/phenology), pas
# les outils de dev (pytest, ruff, pre-commit) ni la stack API S5 (fastapi,
# uvicorn, asyncpg), sans rapport avec l'exécution des DAG.

rasterio>=1.3
shapely>=2.0
geopandas>=0.14
pyproj>=3.6
fiona>=1.9
pyogrio>=0.7
psycopg2-binary>=2.9
python-dotenv>=1.0
requests>=2.31
numpy>=1.26
scipy>=1.11
pandas>=2.1
scikit-learn>=1.4
matplotlib>=3.8
py7zr>=0.20
